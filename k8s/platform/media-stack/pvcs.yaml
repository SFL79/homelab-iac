apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-base
  namespace: media-stack
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: slow-local
  resources:
    requests:
      storage: 500Gi
