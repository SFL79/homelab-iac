apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quota-portal-data
  namespace: quota-portal
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: fast-local
  resources:
    requests:
      storage: 1Gi
