version: "3.8"

services:
  adguardhome:
    image: adguard/adguardhome:v0.107.74
    container_name: adguardhome
    restart: unless-stopped
    network_mode: host
    volumes:
      - /home/sf135/apps/adguard/work:/opt/adguardhome/work
      - /home/sf135/apps/adguard/conf:/opt/adguardhome/conf
